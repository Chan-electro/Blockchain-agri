# Frontend runtime config (Vite will expose VITE_* vars via import.meta.env)
# The deploy script (smart-contract/scripts/deploy.js) rewrites this file
# automatically after each deploy, so you normally don't edit it by hand.

VITE_API_BASE_URL=http://localhost:3001
VITE_CHAIN_NAME=Hardhat #31337
VITE_CONTRACT_ADDRESS=
